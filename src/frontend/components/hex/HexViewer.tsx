interface HexViewerPropTypes {
    title: string;
    binaryData: number[];
}

export function HexViewer(props: HexViewerPropTypes) {
    const { title, binaryData } = props;
    return (
        <div>TODO</div>
    );
}
